---
run_at: "2026-08-06T16:45:00Z"
phase: "phase-007"
advance: "ADV-CONSOLE-002"
outcome: complete
reviewability_score: 86
checks_passed: ["cargo fmt --check", "cargo clippy --workspace --all-targets -- -D warnings", "cargo test --workspace", "cargo build/clippy --target wasm32-unknown-unknown (totem-console)", "arrive doctor artifacts", "arrive plan check", "arrive check --strict", "arrive score"]
checks_failed: []
branch: "advance/sub/phase-007/ADV-CONSOLE-002"
pr: "https://github.com/srswart/totem/pull/39"
---

# Journey report — ADV-CONSOLE-002

## Step 1 — Gate

`git ls-remote --heads origin 'refs/heads/advance/*'` showed legacy
`advance/ADV-*` branches all merged into `origin/master`. Phase branches
003–006 merged into master; phase-007 unmerged, with all four of its
sub-branches (`ADV-INFRA-001`, `ADV-GATEWAY-003`, `ADV-CORE-003`,
`ADV-CURATOR-001`) already merged into `origin/advance/phase-007` — no
gate. Phase-007's plan file showed one `planned` item,
`ADV-CONSOLE-002`, not WORKSTATION- or MODEL-designated, dependencies
(`ADV-CONSOLE-001`, `ADV-CORE-003`, `ADV-GATEWAY-004`) all `done`. CONTINUE
mode, targeting `ADV-CONSOLE-002` on `advance/phase-007`.

## Step 2 — Bootstrap

`./scripts/bootstrap-arrive.sh`: architecture `x86_64`, tarball
`arrive-cli-v0.2.0-linux-x86_64.tar.gz`, installed clean. `arrive 0.2.0`,
enterprise-auth enabled, auth-backends legacy.

## Steps 3–4 — Context and advance selection

Read `CLAUDE.md`, the implementation plan, the agent-rules docs, the project
brief/solution intent, `docs/arrive-decomposition-gaps.md`, and the
tech-direction docs. Read `arrive/systems/058-totem-core/advances/ADV-CONSOLE-002.md`
and `ADV-CORE-003.md` on `origin/advance/phase-007` (not on the locally
checked-out `master`, which was detached at a different commit — checked
out `advance/phase-007` first to read the real state).

Investigation found that `ADV-CORE-003` (already complete) had built the
entire `PromotionRepository` — `propose`/`approve`/`reject`/`demote`/
`pending`/`history`/`proposed_record` — explicitly with this advance in
mind ("the queue ADV-CONSOLE-002 renders"), and `ADV-CURATOR-001` had
similarly built `CurationRepository::history` explicitly for this advance's
audit viewer. `ADV-GATEWAY-004`'s own advance body had flagged exactly the
gap this advance needed to fill: "When one exposes [promotion] (ADV-CONSOLE-002,
or a gateway advance), it must append an entry, and that needs a new
`AccessOperation` variant plus a migration". This made scope concrete
rather than speculative.

## Step 5 — Implementation

Claimed the sub-branch immediately (`chore: [ADV-CONSOLE-002] claim`),
pushed, no collision.

Ten commits, split by architectural layer and by test-then-implementation:

1. `tidy:` `Serialize`/`Deserialize` on `PromotionEvent`/`CurationEvent`
   (needed before either could travel over JSON).
2. `feat:` core — `Governance::resolve` + new `AccessOperation` variants
   (tests written first; failed to compile against the not-yet-existing
   method before the implementation landed in the same commit — see the
   honest TDD note in the advance body about why core/store/gateway are
   claimed `tdd:red-green` and console is not).
3. `test:` store — `governance.rs`'s 8 tests + 2 new `access_log.rs` cases,
   committed against not-yet-existing `pending_review`/`resolve_review`/
   `for_memory` methods (compile failure verified before commit 4).
4. `feat:` store — the three methods + migration 7.
5. `test:` gateway — 10 integration tests in
   `promotion_uncertainty_audit.rs`, committed against not-yet-existing
   endpoints (compile failure verified before commit 6).
6. `feat:` gateway — six new REST endpoints, `error.rs` status mappings.
7. `feat:` console — view models (6 tests).
8. `feat:` console — views + two new tabs (7 SSR tests, using a
   harness-component pattern since Dioxus's `EventHandler` can only be
   constructed from inside a live component, not in test setup code).
9. `feat:` console — `api.rs` wiring (untested by unit test, wasm32-only,
   verified to build/lint clean for `wasm32-unknown-unknown` — the
   `wasm32-unknown-unknown` rustup target was not installed in this sandbox
   and had to be added via `rustup target add`, which succeeded).
10. `docs:` complete advance record and plan status (two commits: the main
    update, then a follow-up fixing the reviewability score placeholder
    once `arrive score` was actually run).

One incidental bug fix noticed and corrected while editing `dto.rs`:
`EnrollResponse`'s `components`/`advances` doc comments were swapped
(pre-existing, unrelated to this advance's behavior).

**Scope correction, disclosed in the advance body:** the Objective's fourth
item ("value/usage reports incl. the 'retire?' queue") is not delivered —
left as an explicit unassigned gap rather than silently expanding scope or
narrowing the Objective without saying so. `demote` and MCP-tool exposure
for propose/approve/reject also remain unwired to any surface; the
Behavioral Change only required propose/approve/reject to be reachable
from the console (REST), which is satisfied.

## Step 6 — Advance file and plan

Frontmatter: `status: complete`, `implementation_completed_at` set,
`components` corrected to include `core`/`store` (mirroring
`ADV-GATEWAY-004`'s own precedent for the same kind of correction),
`reviewability_score: 86`. Body: Objective/Behavioral Change updated to
match what was actually delivered, a new `## Scope Correction` section,
`## Changes Made` fully replaced with dated entries, `## Check for
Understanding` fully replaced with 7 questions grounded in the files
actually changed. `arrive/implementation-plan.yaml`'s `ADV-CONSOLE-002`
item set to `status: done` with a note on the scope correction.
`arrive plan check` — valid.

## Step 7 — Validation

All required checks passed:

- `cargo fmt --check` — clean (after one `cargo fmt` pass to fix
  formatting the editor introduced).
- `cargo clippy --workspace --all-targets -- -D warnings` — clean. One
  real clippy catch along the way: `ProposePromotionResponse::Promoted`'s
  `decision: PromotionEvent` field tripped `large_enum_variant` (544 bytes
  vs. 272) — boxed it, matching how `totem_store::PromotionOutcome` itself
  already boxes the same field.
- `cargo test --workspace` — every crate green, including the new tests
  named above. **Environment note:** the session's disk allowance was
  exhausted mid-validation (`target/debug` had grown to ~30G across the
  session's many incremental compiles); a `cargo clean`-equivalent
  (removing `target/debug/{deps,incremental,build,.fingerprint}`) followed
  by a fresh `CARGO_INCREMENTAL=0 cargo test --workspace` recovered ~28G
  and completed clean. Reported plainly rather than omitted, per the
  "if a tool you needed was unavailable, say so" rule — the *tests*
  themselves were not affected, only the build cache needed reclaiming.
- `cargo build`/`cargo clippy --target wasm32-unknown-unknown` for
  `totem-console` specifically, since `api.rs` (the only place the new
  fetch/callback wiring lives) only compiles on that target and is
  `#[cfg(target_arch = "wasm32")]`-gated out of the default workspace
  build. The target was not pre-installed; `rustup target add
  wasm32-unknown-unknown` succeeded.
- `arrive doctor artifacts` — 38 scanned, 0 issues.
- `arrive plan check` — valid.
- `arrive check --strict` — all gates passed (no resident components
  touched).
- `arrive score` — **86 [RED]** (Size 72, Novelty 4, Risk 10; flag
  `migration`), 155.8 min review estimate, over 10 unpushed commits. Not
  split — same two reasons `ADV-GATEWAY-004` gave for its own
  four-endpoint gap-fill (91 RED): the phase-per-run protocol fixed this
  advance's scope before the run started, and the six new endpoints don't
  decompose cleanly (each pairs one `ops.rs` function, one handler, one DTO
  pair, the same shape `totem_recall`/`totem_save` already establish).

## Step 8 — Sub-PR

Pushed `advance/sub/phase-007/ADV-CONSOLE-002`; no push collision (the
claim commit's window held). Opened
[PR #39](https://github.com/srswart/totem/pull/39), base
`advance/phase-007`, body per the required template (advance ID/objective,
what changed, evidence, reviewability score, what could not be satisfied,
the merge-strategy reviewer note). Subscribed to PR activity and scheduled
a ~1-hour check-in via `send_later`.

## Final report

- Architecture: `x86_64` (per `bootstrap-arrive.sh`'s own reported line).
- Phase `phase-007`, CONTINUE mode, `ADV-CONSOLE-002` — **complete**, with
  an honestly disclosed scope correction (value/usage reports + retire
  queue, and demote/MCP promotion exposure, deferred as unassigned gaps).
- Checks: all passed (`cargo fmt --check`, `cargo clippy --workspace
  --all-targets -- -D warnings`, `cargo test --workspace`, the
  wasm32-unknown-unknown build/clippy pass for `totem-console`, `arrive
  doctor artifacts`, `arrive plan check`, `arrive check --strict`, `arrive
  score`). No failures. One environment hiccup (disk allowance exhausted
  mid-run) worked around by reclaiming `target/debug` build-cache space,
  not by skipping or weakening any check.
- Reviewability: **86 [RED]**. Documented as atomic rather than split, per
  the `## Reviewability` section above.
- Correction made to the advance itself: `components` widened from
  `["console", "gateway"]` to include `core`/`store` (the same shape of
  correction `ADV-GATEWAY-004` made for itself); the Objective's
  value/usage-reports item disclosed as not delivered.
- Invariant established only partially: the Uncertainty-resolution and
  promotion-decision guards are re-checked at the database (not only in
  Rust), but a genuinely simultaneous double-decision race is not
  concurrency-tested — the same disclosed limit `ADV-CORE-003` accepted for
  the analogous promotion-decision race, not a new gap this advance
  introduced.
- Sub-branch: `advance/sub/phase-007/ADV-CONSOLE-002`. Sub-PR:
  https://github.com/srswart/totem/pull/39.
- This journey report is committed on the sub-branch (see below), so it
  merges into the phase branch together with the work it describes.
