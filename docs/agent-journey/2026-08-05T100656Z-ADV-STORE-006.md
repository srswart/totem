---
run_at: "2026-08-05T10:06:56Z"
advance: "ADV-STORE-006"
outcome: complete
reviewability_score: 41
checks_passed: ["cargo fmt --check", "cargo clippy --workspace --all-targets --features totem-store-spike/server-parity -- -D warnings", "cargo test --workspace", "server-parity suite x3", "arrive doctor artifacts", "arrive plan check", "arrive check --strict"]
checks_failed: []
branch: "advance/ADV-STORE-006"
pr: "https://github.com/srswart/totem/pull/4"
---

# Journey: ADV-STORE-006 — server-mode parity, executed and closed

**Not a scheduled cloud run.** This advance was environment-blocked for the
cloud routines (no `surreal` server reachable from the sandbox) and was executed
in an interactive local Claude Code session on the developer's workstation,
which has Docker. The plan item went `blocked` → `done` per the advance's own
instruction that only whoever runs it on a capable host may unblock it.

## How it went

1. The user ran the pre-existing parity test against
   `surrealdb/surrealdb:v3.2.4` in Docker — green. That covered one of seven
   planned-work items; the rest (server-only auth surface, capability defaults,
   HTTP live-query limitation, controls, findings) were implemented in-session.
2. First full run of the extended suite: 2 passed, 3 failed. Two failures were
   over-strict authored assertions (version string vs semver build metadata;
   live-refusal wording). The third was the run's real payoff: the VIEWER-role
   write test *expected a refusal* and the server instead **silently discarded
   the write** — `CREATE` returned OK, persisted nothing.
3. Verified the discard out-of-band (curl token signin as the viewer): CREATE
   `status: OK, result: []`; DDL → loud `IAM error: Not enough permissions`;
   SELECT returned every scope. The test was rewritten to pin those observed
   semantics with read-back evidence in both directions, and the finding became
   TD-011.
4. WS live-query control needed the table defined first (LIVE over WS validates
   the table; the HTTP transport refuses before even looking — itself evidence
   that the refusal is transport-level).
5. Final state: 5 parity tests, 3 consecutive clean runs; workspace suite green;
   findings §5 rewritten as closed with TD-009..TD-011; ADV-STORE-001's
   parity residual risk retired.

## What later advances inherit

- ADV-STORE-001: residual risk retired; scope isolation reconfirmed as a
  store-layer job from the server side (DB roles are not row filters).
- ADV-GATEWAY-003: least-privilege DB users cannot be trusted with error-checked
  writes (TD-011); live feeds need WebSocket or embedded connections (TD-009);
  capability refusal texts differ by engine — never match on wording (TD-010).

## Corrections to the authored advance

- The expected-refusal assertion for viewer writes was wrong about 3.2.4's
  behaviour; corrected to the observed silent-discard semantics. The correction
  is the finding, and is stated in the advance's Evidence section.
