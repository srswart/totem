---
run_at: "2026-08-05T11:29:55Z"
advance: "ADV-STORE-007"
outcome: complete
reviewability_score: 108
checks_passed: ["cargo fmt --check", "cargo clippy --workspace --all-targets -- -D warnings", "cargo clippy -p totem-embedding-spike --features local-model", "cargo test --workspace", "local-model quality test x2", "arrive doctor artifacts", "arrive plan check", "arrive check --strict"]
checks_failed: []
branch: "advance/ADV-STORE-007"
pr: "https://github.com/srswart/totem/pulls (opened after push; see advance frontmatter pr_links)"
---

# Journey: ADV-STORE-007 — recommended embedding model, executed

**Not a scheduled cloud run.** Like ADV-STORE-006, this closed an
environment-blocked gap on the developer's workstation (hub access for the
one-time model download), in an interactive local session, before the cloud
loop reached the advances that depend on the answer.

## Why now

A findings review asked: do completed investigations force decisions on the
next advances? Two did. ADV-STORE-001's HNSW index needs a dimension the
embedding recommendation implied but never measured, and ADV-STORE-002's spec
didn't say what its implementing cloud agent should do about the weight
download it cannot perform. Both were resolved here before either advance
became eligible.

## How it went

1. Added `fastembed =5.17.4` as an optional dependency behind an
   off-by-default `local-model` feature; wrapped BGE-small-en-v1.5 in the
   spike's existing `EmbeddingProvider` trait (Mutex adapts `&mut self`).
2. First run: cold-cache model download took ~276 s, then **5/5 labeled
   queries ranked first — including the paraphrase the hashing baseline
   ranks second** (EMB-001's discriminating case), at ~4.8 ms per call.
   Second run (warm cache, ~124 ms load) reproduced ranking and latency.
3. In-run sensitivity control: the hashing baseline still fails the
   paraphrase under the identical harness.
4. Findings recorded as EMB-004; embeddings.md retitled closed with the pin:
   **BGE-small-en-v1.5, 384 dimensions, cosine** — ADV-STORE-001's index
   derives from an executed assertion, not a spec sheet.
5. All investigation findings bound into the next advances in the same
   branch: DIMENSION 384 + TD-002/004 constraints + TD-011 connection
   identity into ADV-STORE-001; Embedder-trait sandbox split + startup-load
   guidance into ADV-STORE-002; TD-009 into ADV-GATEWAY-001/CONSOLE-001;
   TD-011 invariant onto the `store` component; `docs/tech-direction/` added
   to the cloud notes' binding reading list.

## Score note

108 [RED] — 1,500 of ~1,619 inserted lines are the regenerated `Cargo.lock`
for the one pinned optional dependency; the human-reviewable surface is ~120
lines. Kept whole per the ADV-STORE-004 precedent, justified in the advance's
Reviewability section along with the three heuristic risk-flag hits.

## What later advances inherit

- ADV-STORE-001: an executed 384-dimension pin, binding TD constraints in its
  spec, and the TD-011 store invariant.
- ADV-STORE-002: a measured provider decision and explicit instructions for
  working inside the sandbox's download block (trait split, startup loading,
  bake weights into deployment images).
- The cloud agents: `docs/tech-direction/` is now required reading, so
  findings can no longer be missed by an implementing agent.
