---
run_at: "2026-08-08T16:28:35Z"
phase: "phase-014"
advance: "ADV-CORE-007"
outcome: complete
reviewability_score: 7
checks_passed: ["cargo fmt --check", "cargo test --workspace", "arrive doctor artifacts", "arrive plan check", "arrive check --strict", "arrive score"]
checks_failed: ["cargo clippy --workspace --all-targets -- -D warnings (pre-existing, out of scope)"]
branch: "advance/sub/phase-014/ADV-CORE-007"
pr: "https://github.com/srswart/totem/pull/81"
---

## Architecture

`Architecture: x86_64 (expecting arrive-cli-*-linux-x86_64.tar.gz)` — bootstrap-arrive.sh succeeded, `arrive 0.2.0`.

## Phase, mode, advance

Phase `phase-014` ("Make ranking observable, calibrate it, and clear the
deferred residuals: MSRV, restore drill"), CONTINUE mode. `advance/phase-014`
existed unmerged into master with no unmerged sub-branches (Case C). Its
plan items were mostly `done`; the first non-WORKSTATION `planned` item in
plan order was **ADV-CORE-007** ("Correct the workspace MSRV"), no
dependencies, notes say "Cloud-eligible" (no `MODEL: claude-opus-5` tag) —
Sonnet-eligible, matching this routine.

Implemented **ADV-CORE-007 in full** (complete, not partial).

## What was wrong and what changed

`Cargo.toml` declared `rust-version = "1.85"`. `cargo metadata
--format-version 1` (per-package `rust_version` field, available since a
modern cargo) shows the highest declared requirement across the resolved
dependency graph is `fastnum 0.7.5` at `1.94` (`darling 0.23.0` next, at
`1.88`) — confirming the advance's own claim exactly.

- `Cargo.toml`: `rust-version` → `1.94`, with a comment recording the
  derivation method (max `rust_version` via `cargo metadata`) for the next
  person to re-check.
- `Dockerfile`: the comment above the `rust:1.96-slim-trixie` pin cited the
  old `1.85` figure as the reason for the gap to the pin; updated to cite
  the corrected `1.94`. The pin itself (`1.96`) needed no change — it was
  already ahead of the corrected MSRV.
- Advance file and `arrive/implementation-plan.yaml` updated to `complete`
  / `done`.

## Verification (evidence: msrv:verified)

`rustup toolchain install 1.94.0 --profile minimal` succeeded (network
reachable for this). Ran `rustup run 1.94.0 cargo build --workspace
--locked` twice: once *before* editing `Cargo.toml` (to confirm the
toolchain itself was sufficient, independent of the declared value) and
once *after* (to confirm the edit introduced no regression). Both finished
clean with no errors. This is a genuine build on the exact declared
version, not an inference from reading manifests.

## Checks — passes and failures

Passed: `cargo fmt --check`, `cargo test --workspace` (every crate, 0
failures across ~80 test binaries + doctests), `arrive doctor artifacts`
(0 issues), `arrive plan check`, `arrive check --strict`, `arrive score`.

Failed: `cargo clippy --workspace --all-targets -- -D warnings`, on
`crates/totem-gateway/src/auth.rs:616` — `clippy::collapsible_if` against
code from ADV-GATEWAY-013 (`git blame` confirms), unrelated to this
advance and predating it. Verified this is not a disk-space artifact by
re-running after freeing disk (see below) — the failure reproduces
identically. Not fixed here: out of scope per the advance's own Scope and
Boundaries ("the declared MSRV and its consistency with the deployment
pin... out of scope: an MSRV policy"), and fixing unrelated code would
break this advance's atomicity for a 7-point-GREEN diff. Flagged in the
sub-PR body as a follow-up candidate.

## Reviewability

**7 [GREEN]** (`arrive score --base origin/advance/phase-014`). No split
needed.

## Corrections to the advance

None — the advance's own diagnosis (fastnum 0.7.5 needs 1.94, darling 0.23
needs 1.88) was independently re-derived via `cargo metadata` and matched
exactly.

## Invariants established only partially

None applicable — this advance touches manifest/metadata only, not a
security-critical boundary.

## Operational note (not part of the advance, recorded for the next run)

Mid-run, `cargo test --workspace` failed with "No space left on device":
the session's disk allowance was exhausted by `target/` growing to ~29GB
(triple-built: `cargo build` with the default 1.94.1 toolchain, then again
explicitly with 1.94.0, plus `cargo test`/`cargo clippy` artifacts all
sharing one `target/` dir). Freed by `rm -rf target/` (safe — build
artifacts only), which recovered 29GB and let every subsequent check run
clean. Worth knowing for future MSRV-adjacent or multi-toolchain work in
this sandbox: a second toolchain's build output roughly doubles `target/`
size for that crate graph.

## Branch and PR

`advance/sub/phase-014/ADV-CORE-007` → https://github.com/srswart/totem/pull/81
(base: `advance/phase-014`).

## Journey report

This file, committed on the sub-branch.
