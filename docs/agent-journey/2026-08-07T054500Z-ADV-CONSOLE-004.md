---
run_at: "2026-08-07T05:45:00Z"
phase: "phase-008"
advance: "ADV-CONSOLE-004"
outcome: complete
reviewability_score: 68
checks_passed: ["cargo fmt --check", "cargo clippy (native + wasm32)", "cargo test -p totem-console --lib (28)", "cargo test --workspace", "arrive doctor artifacts", "arrive plan check", "arrive check --strict"]
checks_failed: []
branch: "advance/sub/phase-008/ADV-CONSOLE-004"
pr: "(sub-PR into advance/phase-008 — link on the PR)"
---

# Journey report — ADV-CONSOLE-004 (workstation, second WORKSTATION advance)

Workstation session (Shawn + Claude Code), claim-first on the sub-branch.
Notable: the claim followed a same-day incident — a stranded PR amendment
had left this advance's WORKSTATION designation off the phase branch, and a
manually-fired cloud run claimed it before being stopped (see the
observation log). The designation was restored to the phase branch before
this work began.

Tailwind v4 design system delivered with one recorded toolchain correction:
dx 0.7.3's native Tailwind integration hangs on install and blocks the dev
build entirely; the official CLI + a committed compiled stylesheet
(inlined via include_str!) replaced it, keeping every build path free of a
CSS toolchain. Red-first SSR tests pin the semantic surface (badge/shell
classes), 28 tests green, both targets clean, live browser verification
against the durable store with before/after screenshots committed as
evidence. Score 68 RED documented: size is generated-artifact + screenshot
dominated, risk 0; the hand-authored diff is the review surface.

Taste sign-off: Shawn reviewed the running app ("fine for the moment, will
refine as we go") — refinement is expected via small follow-ups, not
blocked on this advance.
