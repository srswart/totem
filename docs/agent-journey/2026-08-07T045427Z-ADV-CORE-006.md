---
run_at: "2026-08-07T04:51:15Z"
phase: "phase-008"
advance: "ADV-CORE-006"
outcome: complete
reviewability_score: 80
checks_passed: ["cargo fmt --check", "cargo clippy --workspace --all-targets -- -D warnings", "cargo test --workspace", "arrive doctor artifacts", "arrive plan check", "arrive check --strict"]
checks_failed: []
branch: "advance/sub/phase-008/ADV-CORE-006"
pr: "https://github.com/srswart/totem/pull/45"
---

# Journey report: ADV-CORE-006

## Architecture line

`Architecture: x86_64 (expecting arrive-cli-*-linux-x86_64.tar.gz)` — bootstrap succeeded on the first attempt, exit 0.

## Phase, mode, advance

Phase `phase-008` ("Enablement (evaluation tooling first)"), **CONTINUE mode**. The Step 1 gate found `advance/phase-008` unmerged into master with all three of its existing sub-branches (`ADV-STORE-005`, `ADV-GATEWAY-008`, `ADV-GATEWAY-009`) already merged into the phase branch, and no item in the phase at `in_progress`. The first `planned` item in plan order, skipping WORKSTATION-designated items, was `ADV-CORE-006` — no `MODEL: claude-opus-5` note, so Sonnet-eligible. Implemented **complete**.

## Check results

All pass:
- `cargo fmt --check`
- `cargo clippy --workspace --all-targets -- -D warnings`
- `cargo test --workspace` — every crate green, including the pre-existing suites this advance's type change touched (`totem-curator/tests/dedupe.rs`, `totem-gateway/tests/recall_and_save.rs`) after updating their assertions for `actor`/`harness` becoming `Option`
- `arrive doctor artifacts` — 38 files scanned, 0 issues
- `arrive plan check` — valid
- `arrive check --strict` — no resident components touched (none of the affected components — `core`, `store`, `gateway`, `curator` — are resident; all are stage `incubating`)

No failures at any step. `ci:passed` is **not** claimed — no pipeline result was read from this run.

## Reviewability

**80 [RED]** (size 60, novelty/risk — risk 20; flags `auth`, `migration`), computed with `arrive score --base origin/advance/phase-008`. **Documented in the advance's new `## Reviewability` section, not split.** The change spans `totem-core` (the new `AccessOperation::Refused`/`RefusalReason` vocabulary and `Option` identity fields), `totem-store` (an additive schema migration and its row mapping), and `totem-gateway` (the actual logging wiring across every REST and MCP refusal point), and none of the natural cut lines produce two shippable halves — core/store-first leaves the new variant as dead code with nothing constructing it; gateway-first does not compile, since the gateway's refusal-logging call sites are written directly against the new core types. Full breakdown and suggested review order are in the advance file.

## Corrections made to the advance

None. The advance's Objective, Behavioral Change, and Planned Implementation Tasks matched what was actually buildable without needing a factual correction — ADV-GATEWAY-003 had already scoped this residual precisely (`AuthError`'s existing 401/403 split, the fingerprint concept, the "log write is the only store touch" framing all came from that advance's own disclosed residual and translated directly).

One planned task was **honestly skipped and explained rather than fabricated**: the advance's checklist asked for a `tidy:` preparatory commit, and none exists on the sub-branch. There was no pre-existing duplication to extract ahead of this change — the `authorize_identity`/`authorize_scope`/`authorize_repo` helpers that *do* reduce duplication in `ops.rs` only make sense once they also log on failure, so building them *is* the feature, not preparation for it. The advance's Planned Implementation Tasks and Evidence sections both say this plainly rather than leaving the checkbox silently unchecked or checking it falsely.

## Invariants established only partially

Two, both stated plainly in the advance's `## Risk + Rollback`:

1. **"A log-write failure never turns a refusal into a success" is honesty-based, not test-proven.** Every refusal path logs *before* returning the error, and a log-write failure is caught and reported via `eprintln!` rather than propagated — but this codebase has no injectable store-failure seam yet, so no test actually forces the store write to fail and asserts the refusal still stands. The code shape makes it true; nothing exercises the failure branch.
2. **The write-amplification / flood risk is noted, not solved.** An unauthenticated caller hammering a route now produces one refusal row per attempt, with no rate limiting or coalescing. This was explicitly out of scope per the advance's own Scope and Boundaries (deferred to a future rate-limiting advance) and is called out again in Risk + Rollback rather than silently left unmentioned.

## Sub-branch and sub-PR

Branch: `advance/sub/phase-008/ADV-CORE-006`
PR: https://github.com/srswart/totem/pull/45 (base `advance/phase-008`)

Claimed cleanly — the empty claim commit pushed with no collision, so this run never contended with another for the same advance.

## Journey report

This file, committed on the sub-branch with prefix `docs:` and pushed, so it merges into the phase branch (and eventually master) alongside the work it describes.
