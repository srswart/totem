---
run_at: "2026-08-07T03:17:00Z"
phase: "phase-008"
advance: "ADV-GATEWAY-009"
outcome: complete
reviewability_score: 55
checks_passed: ["cargo fmt --check", "cargo clippy --workspace --all-targets -- -D warnings", "cargo test --workspace", "arrive doctor artifacts", "arrive plan check", "arrive check --strict", "arrive score"]
checks_failed: []
branch: "advance/sub/phase-008/ADV-GATEWAY-009"
pr: "https://github.com/srswart/totem/pull/43"
---

## Final report

1. **Architecture**: `x86_64` (`arrive-cli-*-linux-x86_64.tar.gz`), `arrive 0.2.0`, bootstrap OK.

2. **Phase / mode / advance**: `advance/phase-008`, **CONTINUE** mode (a sub-branch claim already existed for none of phase-008's remaining items — the phase branch itself was unmerged into master with two prior sub-branches, `ADV-STORE-005` and `ADV-GATEWAY-008`, both already merged into `origin/advance/phase-008`). Implemented **ADV-GATEWAY-009** in full — status `complete`.

3. **Check results**:
   - `cargo fmt --check` — pass (after one `cargo fmt` pass, captured as its own `tidy:` commit).
   - `cargo clippy --workspace --all-targets -- -D warnings` — pass, zero warnings.
   - `cargo test --workspace` — pass. Notably caught a real schema gap mid-implementation: `LandscapeRepository::sync`'s `UPSERT CONTENT` with the new `git_repo` field failed against the real `repo` table (`SCHEMAFULL`, no such field defined) in `totem-arrive-sync`'s dogfood test — required adding schema migration 8 (`repo_git_identity`) rather than just a Rust-level struct field. Full suite green after that fix.
   - `arrive doctor artifacts` — 38 files scanned, 0 issues (the new `registry.yaml` field and the advance frontmatter both parse cleanly).
   - `arrive plan check` — valid.
   - `arrive check --strict` — no changes detected (no resident components touched; all touched components — `gateway`, `store`, `arrive-sync` — are `incubating`).
   - `arrive score` — **55 [YELLOW]** (size 30, novelty 0, risk 25; flags `auth`, `migration`, `concurrency`).

4. **Reviewability**: 55 [YELLOW], within the ≤60 budget. Not split — no `## Reviewability` justification section was needed since it isn't Red.

5. **Corrections made to the advance**:
   - The advance's original Risk + Rollback text named a non-existent `AuthError::RepoMismatch`. Corrected to the actual variant reused, `AuthError::RepoNotBound` (already `authorize_identity`'s project-mismatch error — it fit this refusal unchanged, so no new `AuthError` variant was needed).
   - The advance's Behavioral Change bullet framed the id-unification decision as strictly either/or ("the landscape snapshot carries the `owner/name` repo id, **or** credentials carry both ids"). What was actually built is additive: the `repo` table keeps its existing ARRIVE-id primary key (`repo.id`, e.g. `"058-totem"`) unchanged — preserving `GET /landscape/:repo`'s existing contract for the console and this repo's own dogfood test, neither of which is a declared component of this advance — and gains a new `git_repo` field carrying the `owner/name` id credentials already speak. Corrected the advance body to describe this rather than leave the stale either/or framing standing.

6. **Invariants established only partially**: none knowingly. The one edge case worth naming: `GET /landscape/:repo`/`totem_landscape` for a repo that has *never* synced (or whose row predates this migration and hasn't been re-synced) falls back to comparing the credential against the raw ARRIVE-id path segment rather than a confirmed `owner/name` id — deliberately, so an unconfirmed binding refuses exactly like a real mismatch (proven by `a_bound_token_cannot_confirm_a_never_synced_repo_s_binding` and its MCP twin). This is the intended behavior, not a shortcut, but it's worth a reviewer's eye: a credential can never read *any* repo's landscape by ARRIVE id alone until that repo has synced under a matching `git_repo` at least once.

7. **Branch / PR**: `advance/sub/phase-008/ADV-GATEWAY-009` → https://github.com/srswart/totem/pull/43 (base `advance/phase-008`).

8. **Journey report**: this file, committed on the sub-branch as `docs: agent journey report for ADV-GATEWAY-009` and pushed — the open sub-PR picks it up automatically.

## Commit series (in order)

1. `chore: [ADV-GATEWAY-009] claim`
2. `test: [ADV-GATEWAY-009] repo-mismatch refusals for enroll and landscape (REST + MCP)` — does not compile by itself (captured red, matching ADV-GATEWAY-003's convention).
3. `feat: [ADV-GATEWAY-009] unify repo id spaces and bind enroll/landscape to the credential`
4. `tidy: [ADV-GATEWAY-009] cargo fmt`
5. `docs: [ADV-GATEWAY-009] complete advance record`
6. `docs: agent journey report for ADV-GATEWAY-009` (this commit)

## Notes for whoever reviews the sub-PR

- The commit-2 "red" state is a genuine compile failure across every crate that constructs a `RepoArtifact` literal, not merely a failing assertion — consistent with how ADV-GATEWAY-003's own evidence log captured red (an unresolved-import compile error), which this run treated as precedent rather than improvising a different convention.
- Schema migrations in this codebase are strictly append-only (`migrate.rs`'s own doc: "editing a migration that has already run somewhere leaves that database on a schema no entry describes"), so the `git_repo` field could not be added to migration 1's `DEFINE TABLE repo` statement — it required a new migration 8, only discovered once `cargo test --workspace` hit a live `SCHEMAFULL` rejection in `totem-arrive-sync`'s dogfood test.
- `console` is untouched: its `GET /landscape/:repo` reads and its `"058-totem"` fixture default keep working exactly as before, since the ARRIVE-id primary key never changed.
